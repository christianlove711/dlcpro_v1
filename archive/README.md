# 归档目录

此目录保存整理前仍有追溯价值、但不参与当前软件运行与打包的资料。

- `fpga_history/`：旧交付工程、PL UDP阶段实验、RGMII报告和回退资料；
- `non_project_simulations/`：明确标记为非当前项目的虚拟仿真；
- `development_previews/`：旧UI截图、PDF页面提取和临时预览配置；
- `generated_legacy_2026-08-05/`：整理前的旧 `build/dist`、缓存和临时状态文件。

归档采用无损移动，未删除旧可执行程序或FPGA资料。当前发布流程明确排除此目录。
